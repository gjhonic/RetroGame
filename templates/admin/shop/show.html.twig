{% extends 'admin/admin_base.html.twig' %}

{% block title %}Жанры{% endblock %}

{% block body %}
    <h1>Shop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ shop.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ shop.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ shop.description }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ shop.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_shop_index') }}">back to list</a>

    <a href="{{ path('admin_shop_edit', {'id': shop.id}) }}">edit</a>

    {{ include('admin/shop/_delete_form.html.twig') }}
{% endblock %}
