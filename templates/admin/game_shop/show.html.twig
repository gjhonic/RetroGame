<!DOCTYPE html>

<title>GameShop</title>

{% block body %}
    <h1>GameShop</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ game_shop.id }}</td>
            </tr>
            <tr>
                <th>Link_game_id</th>
                <td>{{ game_shop.linkGameId }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ game_shop.name }}</td>
            </tr>
            <tr>
                <th>Link</th>
                <td>{{ game_shop.link }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_game_shop_index') }}">back to list</a>

    <a href="{{ path('app_game_shop_edit', {'id': game_shop.id}) }}">edit</a>

    {{ include('game_shop/_delete_form.html.twig') }}
{% endblock %}
