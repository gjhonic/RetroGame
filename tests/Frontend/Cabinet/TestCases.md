# Тест-кейсы: Vue-компоненты кабинета/сайта

Список проверяемых сценариев для `assets/vue/Cabinet/`. При добавлении
нового кейса — дописывайте сюда строку с методом, где он проверяется.

Отдельного модуля `Public` больше нет — шаблон кабинета (`templates/cabinet/base.html.twig`)
используется для всего сайта (доступен и неавторизованным посетителям, пункты
меню, требующие входа, скрыты через `is_granted('ROLE_USER')`). Каталог игр,
витрина своих игр, вход и регистрация физически живут в `Controller/Cabinet`
и `assets/vue/Cabinet`, хотя доступны без авторизации.

## AvatarUpload.spec.js

| Кейс | Метод теста |
|---|---|
| Аватар не задан → показывается инициал письма | `показывает инициал письма, когда аватар не задан` |
| Аватар задан → `<img>` с ведущим слэшем в `src` | `показывает текущий аватар с ведущим слэшем` |
| Подходящий файл (100×100 JPG) → POST-запрос multipart, сообщение об успехе | `загружает подходящий файл и показывает сообщение об успехе` |
| Файл больше 400×400px → запрос не отправляется, показана ошибка | `отклоняет файл, превышающий 400×400px, не отправляя запрос` |
| Неподдерживаемый MIME-тип → ошибка без похода в Image/fetch | `отклоняет неподдерживаемый формат файла без похода в Image/fetch` |
| Ответ сервера 422 → показана ошибка из `errors.file` | `показывает ошибку сервера при 422` |
| Сетевой сбой при загрузке → общая ошибка | `показывает общую ошибку при сетевом сбое во время загрузки` |

## PasswordChangeForm.spec.js

| Кейс | Метод теста |
|---|---|
| Валидная форма → PATCH `/api/cabinet/profile/password` с текущим/новым паролем | `отправляет текущий и новый пароль на PATCH /api/cabinet/profile/password` |
| Новый пароль и подтверждение не совпадают → запрос не отправляется | `не отправляет запрос, если новый пароль и подтверждение не совпадают` |
| Ответ 422 → показаны ошибки по полям | `показывает ошибки по полям при 422` |
| Сетевой сбой → общая ошибка | `показывает общую ошибку при сетевом сбое` |

## GameCatalog.spec.js

Каталог доступен без авторизации по адресу `/`; ссылки карточек ведут на `/games/...`.

| Кейс | Метод теста |
|---|---|
| Загрузка первой страницы, рендер карточек (включая бейдж популярности) | `загружает первую страницу и рендерит карточки игр` |
| Номер страницы читается из `?page=` при монтировании | `читает номер страницы из query-параметра ?page= при монтировании` |
| Пустое состояние без активных фильтров | `показывает пустое состояние без игр (без активных фильтров)` |
| Состояние ошибки | `показывает ошибку при неудачном запросе` |
| Переход на следующую страницу обновляет URL | `переходит на следующую страницу и обновляет URL` |
| Пагинация не рендерится при totalPages=1 | `не даёт уйти на страницу за пределами диапазона` |
| ≤7 страниц: кнопки всех страниц подряд, без многоточия | `при малом числе страниц показывает кнопки всех страниц без многоточия` |
| Много страниц, текущая в начале: окно 1–5 + многоточие + последняя | `при большом числе страниц показывает окно вокруг текущей с многоточием до последней` |
| Текущая страница в середине диапазона: многоточие с обеих сторон окна, активная кнопка подсвечена | `при странице в середине диапазона показывает многоточия с обеих сторон` |
| Мобильный `matchMedia('(max-width: 600px)')` → окно в 1 страницу вместо 5 ("1 … текущая … N") | `на мобильных экранах (max-width: 600px) показывает компактную пагинацию` |
| Клик по номеру страницы в пагинации загружает эту страницу | `клик по номеру страницы переходит на неё` |
| Справочник фильтров (`/api/games/filters`) заполняет селекты жанров/платформ | `заполняет селекты жанров/платформ из /api/games/filters` |
| Фильтр по жанру уходит в запрос обрезанным, страница сбрасывается на первую | `применяет фильтр по жанру и сбрасывает страницу на первую` |
| Поиск по названию применяется через debounce (400мс) | `дебаунсит поиск по названию` |
| Кнопка "Сбросить" видна только при активных фильтрах/нестандартной сортировке и очищает их | `кнопка "Сбросить" появляется при активных фильтрах и очищает их` |

## GameDetail.spec.js

Тэйки видны всем, но кнопка "Добавить тэйк" показывается только при
`isAuthenticated: true` (проп из Twig, `is_granted('ROLE_USER')`) — иначе
вместо неё ссылка на `/login`. `mountGameDetail()` по умолчанию монтирует
с `isAuthenticated: true`, чтобы не дублировать проп в каждом тесте.

| Кейс | Метод теста |
|---|---|
| Запрос игры по slug, рендер деталей | `запрашивает игру по slug и рендерит подробности` |
| Состояние ошибки загрузки игры | `показывает ошибку, если игра не найдена` |
| Список тэйков игры загружается через `GET /api/takes?filters[game]=<id>` и рендерится | `загружает и показывает список тэйков игры` |
| Пустое состояние, если тэйков нет | `показывает пустое состояние, если тэйков ещё нет` |
| Кнопка "Добавить тэйк" открывает модалку `TakeCreateModal` | `кнопка "Добавить тэйк" открывает модалку создания` |
| Событие `created` от модалки добавляет тэйк в начало списка и закрывает модалку | `добавляет созданный тэйк в начало списка и закрывает модалку` |
| Без `isAuthenticated` — кнопка скрыта, вместо неё ссылка на `/login` | `без isAuthenticated кнопка "Добавить тэйк" скрыта, вместо неё ссылка на вход` |
| Без `isAuthenticated` — кнопки лайк/дизлайк отключены (`disabled`) | `кнопки лайк/дизлайк отключены для анонимного посетителя` |
| Клик по лайку → `PUT /api/cabinet/takes/{id}/reaction`, счётчик и подсветка обновляются из ответа | `клик по лайку отправляет PUT и обновляет счётчики/подсветку` |
| Повторный клик по уже активной реакции → `DELETE /api/cabinet/takes/{id}/reaction`, подсветка снимается | `повторный клик по уже активной реакции снимает её через DELETE` |
| Клик по счётчику комментариев → `GET /api/takes/{id}/comments`, список рендерится | `клик по счётчику комментариев подгружает и показывает список` |
| Отправка комментария → `POST /api/cabinet/takes/{id}/comments`, добавляется в список, счётчик растёт | `отправка нового комментария добавляет его в список и увеличивает счётчик` |
| Анонимному посетителю вместо формы комментария — ссылка на `/login` | `анонимному посетителю вместо формы комментария показывается ссылка на вход` |

## TakeCreateModal.spec.js

| Кейс | Метод теста |
|---|---|
| Валидная отправка → `POST /api/cabinet/takes`, эмит `created` при 201 | `отправляет текст на POST /api/cabinet/takes и эмитит created при 201` |
| Ответ 422 → показаны ошибки по полям, `created` не эмитится | `показывает ошибки по полям при 422` |
| Сетевой сбой → общая ошибка | `показывает общую ошибку при сетевом сбое` |
| Крестик/кнопка "Отмена"/клик по оверлею → эмит `close` | `эмитит close по клику на крестик, кнопку "Отмена" и клику по оверлею` |
| Клавиша Escape → эмит `close` | `эмитит close по клавише Escape` |

## OurGameList.spec.js

Витрина своих игр — доступна без авторизации по адресу `/our-games`.

| Кейс | Метод теста |
|---|---|
| Запрос `GET /api/our-games`, рендер карточки со ссылкой на страницу игры | `запрашивает список у публичного API и рендерит карточку игры со ссылкой на страницу игры` |
| Состояние ошибки при неудачном запросе | `показывает состояние ошибки при неудачном запросе` |
| Пустое состояние, если опубликованных игр нет | `показывает сообщение, если опубликованных игр нет` |

## OurGameDetail.spec.js

Раскладка: слева от скриншотов — системная информация (без описания),
описание — под скриншотами на всю ширину (см. `.our-game-detail__body`
в `assets/styles/public/our-game.css`). Блок "Посты" — карточки с картинкой
слева, типом/заголовком/описанием справа; описание — HTML из редактора
в админке, санитизируется через DOMPurify перед `v-html`. Модалка поста —
класс `.our-game-post-modal` (70vw). Кнопка "Смотреть таблицу лидеров" под
описанием видна только при `props.slug === 'die-again'` (жёстко зашитая
проверка — таблица `score_die_again`/`GET /api/score-die-again` посвящена
одной конкретной игре, не общая для всех "своих игр"), открывает модалку
`.our-game-leaderboard-modal` (тоже 70vw, как у поста). Первая страница —
10 записей (`GET /api/score-die-again?page=1`, `PER_PAGE=10` на бэкенде),
таблица — в скроллируемой обёртке `.leaderboard-table-wrapper`
(`max-height: 60vh`), кнопка `.leaderboard-load-more button` дозагружает
следующие страницы и накапливает результаты без переоткрытия модалки.

| Кейс | Метод теста |
|---|---|
| Запрос `GET /api/our-games/{slug}`, баннер, `document.title`, системная инфа расположена перед скриншотами в DOM | `запрашивает игру у публичного API, показывает баннер и системную инфу слева от скриншотов` |
| Описание — под скриншотами, не в боковом блоке | `описание отображается под скриншотами, а не в боковом блоке` |
| Состояние ошибки загрузки | `показывает ошибку при неудачном запросе` |
| Без баннера — используется обложка, без обложки и баннера — плейсхолдер | `без баннера использует обложку, без обложки — плейсхолдер` |
| Клик по скриншоту открывает лайтбокс | `рендерит скриншоты и открывает лайтбокс по клику` |
| Блок "Скачать" — ссылка с подписью платформы | `показывает ссылку на скачивание с подписью платформы` |
| Ссылка на трейлер, если задана | `показывает ссылку на трейлер, если она задана` |
| Блок "Посты" — запрос `filters[game]={id}`, карточка с заголовком и HTML-описанием (жирный текст рендерится как `<strong>`) | `запрашивает опубликованные посты по игре и рендерит карточки с заголовком и HTML-описанием` |
| Блок "Посты" — пустое состояние | `показывает сообщение, если постов ещё нет` |
| Кнопка таблицы лидеров видна для die-again | `кнопка "Смотреть таблицу лидеров" видна только для игры die-again` |
| Кнопки таблицы лидеров нет для других игр | `для других игр кнопки таблицы лидеров нет` |
| Клик по кнопке → `GET /api/score-die-again?page=1`, рендер строк таблицы (в т.ч. форматирование времени `m:ss`), без кнопки "Загрузить ещё" при одной странице | `клик по кнопке запрашивает первую страницу /api/score-die-again и показывает таблицу` |
| Несколько страниц → кнопка "Загрузить ещё" видна, клик подгружает `page=2` и добавляет строки без дублей, после последней страницы кнопка исчезает | `при нескольких страницах показывает "Загрузить ещё", клик подгружает следующую страницу` |
| Ошибка на "Загрузить ещё" не скрывает уже показанные строки, показывается инлайн-сообщение | `ошибка при "Загрузить ещё" не скрывает уже показанные результаты` |
| Пустая таблица лидеров | `показывает пустое состояние, если результатов ещё нет` |
| Ошибка загрузки таблицы лидеров (первая страница) | `показывает ошибку при неудачном запросе таблицы лидеров` |
| Закрытие модалки лидеров по крестику | `закрывает модалку лидеров по клику на крестик` |
| Клик по карточке — широкая модалка (`.our-game-post-modal`) с заголовком и полным HTML-описанием (`GET /api/our-game-posts/{id}`) | `клик по карточке открывает широкую модалку с заголовком и полным описанием поста` |
| Пост типа `major_update` получает класс `.our-game-post-card--major` (подсветка), остальные — нет | `пост о крупном обновлении выделяется классом --major` |
| `<script>` в HTML-описании поста вырезается DOMPurify перед рендером | `санитизирует HTML в описании поста (убирает script)` |
| Закрытие модалки поста по крестику | `закрывает модалку по клику на крестик` |

## RegistrationForm.spec.js

| Кейс | Метод теста |
|---|---|
| Успешная регистрация (201): запрос уходит на `/api/register`, показывается экран успеха с кнопкой "Войти" (`.btn--primary`) | `отправляет данные формы на /api/register и показывает успех при 201` |
| Ошибки валидации (422): показываются под соответствующими полями | `показывает ошибки по полям при 422` |
| Email уже занят (409): показывается сообщение о конфликте | `показывает сообщение о занятом email при 409` |
| Сетевой сбой: показывается общая ошибка | `показывает общую ошибку при сетевом сбое` |
